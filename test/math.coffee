require("../math").pollute()

console.log global

describe "Point", ->
  it "should exist", ->
    assert Point

describe "Matrix", ->
  it "should exist", ->
    assert Matrix

describe "Random", ->
  it "should exist", ->
    assert Random
